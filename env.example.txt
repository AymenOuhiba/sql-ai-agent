# Text-to-SQL Environment Variables
# Copy this file to .env and fill in your values
# Never commit .env to GitHub!

# ── AI Backend ─────────────────────────────────────────────────────────────────
# Options: ollama (default, offline), openai, anthropic
AYMENOU_LLM_BACKEND=ollama

# Ollama model to use (must be pulled first: ollama pull llama3.2)
AYMENOU_OLLAMA_MODEL=llama3.2

# Optional custom Ollama API URL
AYMENOU_OLLAMA_URL=http://localhost:11434/api/generate

# OpenAI API key (only needed if AYMENOU_LLM_BACKEND=openai)
# AYMENOU_OPENAI_API_KEY=sk-...

# Anthropic API key (only needed if AYMENOU_LLM_BACKEND=anthropic)
# AYMENOU_ANTHROPIC_API_KEY=sk-ant-...

# ── Database ────────────────────────────────────────────────────────────────────
# SQLite path (default app)
AYMENOU_DB_PATH=data/sample.db

# Safety: maximum rows returned per query
AYMENOU_MAX_QUERY_ROWS=500

# Flask debug mode for web_app_v2.py (0=off, 1=on)
AYMENOU_FLASK_DEBUG=0

# SQL Server settings (for agent.py)
# Replace with your own server name
AYMENOU_SQL_SERVER=localhost\SQLEXPRESS
AYMENOU_SQL_DATABASE=YouTubeAnalytics
