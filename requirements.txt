# Core requirements (always needed)
flask
requests
python-dotenv

# SQL Server connection (for agent.py)
pyodbc

# Optional: cloud AI backends
# openai
# anthropic

# Optional: cloud databases
# psycopg2-binary   # PostgreSQL
# google-cloud-bigquery  # BigQuery
